{% extends 'partials/_master.twig' %}

{% block main %}

    <div class="content">
        <div class="full">
            {{ page.teaser }}
            {{ page.body }}
        </div>
    </div>

{% endblock main %}
