{% setcontent banner = 'banners/' ~ record.banner|default(1) %}
<header>
    <div class="banner">
        <div class="image left"
             style="background-image:url({{ banner.left_image|image }})"/></div>
        <div class="image right"
             style="background-image:url({{ banner.right_image|image }})"></div>
    </div>  
    <a class="logo" href=""><img src="{{ asset('img/logo.svg', 'theme') }}" alt="Mike Dilger - Broadcaster &amp; Naturalist"/></a>       
    <nav>{{ menu('main', 'partials/_menu.twig')}} </nav>
</header>
